server:
  port: ${PORT:3091}
  domain: http://127.0.0.1:3091

spring:
  datasource:
    url: jdbc:mysql://:/${DB:pighand_aio}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      web-stat-filter:
        enabled: true
        url-pattern: /*
        exclusions: "*.gif,*.png,*.jpg,*.html,*.js,*.css,*.ico,/druid/*"
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        reset-enable: true
        login-username: admin
        login-password: ${DRUID_PWD:123456}
      filter:
        slf4j:
          enabled: true
          statement-log-enabled: false
          connection-log-enabled: false
          result-set-log-enabled: true
          statement-executable-sql-log-enable: true
    redis:
      database: 0
      host: 127.0.0.1
      port: 6379
      password:
      jedis:
        pool:
          max-active: 20
          max-wait: 0
          max-idle: 5
          min-idle: 1
#      timeout: 100000

upload:
  path: /Users/wangshuli/Downloads

pay:
  notify-url: http://ja833c76.natappfree.cc/dashboard/order/notify/